import {test} from 'node:test';import assert from 'node:assert/strict';import {createProject,validateProject,migrateProject} from '../src/model.ts';
test('new sessions are quieter while existing projects retain their loudness',()=>{assert.equal(createProject().targetLufs,-24);const p=createProject();p.targetLufs=-18;assert.equal(migrateProject(p).targetLufs,-18);});
test('Deep Mask and advanced background round-trip without losing controls',()=>{const p=createProject();p.variant='DEEP';p.targetLufs=-32;p.background={...p.background,muted:false,solo:false,highpassHz:100,lowpassHz:8000,pan:.3,fadeIn:2,fadeOut:3,speed:.8,reverse:true};assert.deepEqual(validateProject(p),p);});
test('rejects malformed advanced controls and preserves older background records',()=>{const p=createProject();assert.deepEqual(validateProject(p).background,p.background);for(const patch of [{pan:2},{muted:'yes'},{speed:0},{fadeIn:-1},{highpassHz:9000,lowpassHz:1000},{lowpassHz:30000}])assert.throws(()=>validateProject({...p,background:{...p.background,...patch}}));});
